# Demo build config — loaded by `--mode demo` (npm run dev:demo / build:demo).
# Local mirror of the VITE_DEMO_MODE var set on the shared Vercel deployment.
# No secrets here; safe to commit.
VITE_DEMO_MODE=1
